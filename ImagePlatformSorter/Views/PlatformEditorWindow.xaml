<Window x:Class="ImagePlatformSorter.Views.PlatformEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактор площадки"
        Height="420"
        Width="520"
        MinHeight="400"
        MinWidth="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,8"
                   FontWeight="SemiBold"
                   Text="Название площадки" />
        <TextBox Grid.Row="1"
                 Margin="0,0,0,14"
                 Text="{Binding PlatformName, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Grid.Row="2">
            <TextBlock Margin="0,0,0,8"
                       FontWeight="SemiBold"
                       Text="Размеры" />
            <TextBlock Margin="0,0,0,8"
                       Foreground="#FF666666"
                       Text="Укажите размеры через запятую, пробел или с новой строки. Пример: 160x600, 240x400, 300x600" />
            <TextBox AcceptsReturn="True"
                     Height="180"
                     Text="{Binding SizesText, UpdateSourceTrigger=PropertyChanged}"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" />
        </StackPanel>

        <StackPanel Grid.Row="3"
                    Margin="0,16,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Width="110"
                    Margin="0,0,12,0"
                    Click="Cancel_Click"
                    Content="Отмена" />
            <Button Width="110"
                    Click="Save_Click"
                    Content="Сохранить" />
        </StackPanel>
    </Grid>
</Window>


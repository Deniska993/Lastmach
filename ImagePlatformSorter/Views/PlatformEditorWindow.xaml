<Window x:Class="ImagePlatformSorter.Views.PlatformEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Р РµРґР°РєС‚РѕСЂ РїР»РѕС‰Р°РґРєРё"
        Height="460"
        Width="520"
        MinHeight="440"
        MinWidth="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,8"
                   FontWeight="SemiBold"
                   Text="РќР°Р·РІР°РЅРёРµ РїР»РѕС‰Р°РґРєРё" />
        <TextBox Grid.Row="1"
                 Margin="0,0,0,14"
                 Text="{Binding PlatformName, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2"
                   Margin="0,0,0,8"
                   FontWeight="SemiBold"
                   Text="РњР°РєСЃРёРјР°Р»СЊРЅС‹Р№ РІРµСЃ С„Р°Р№Р»Р° (РљР‘)" />
        <TextBox Grid.Row="3"
                 Margin="0,0,0,14"
                 Text="{Binding MaxFileSizeKbText, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Grid.Row="4">
            <TextBlock Margin="0,0,0,8"
                       FontWeight="SemiBold"
                       Text="Р Р°Р·РјРµСЂС‹" />
            <TextBlock Margin="0,0,0,8"
                       Foreground="#FF666666"
                       Text="РЈРєР°Р¶РёС‚Рµ СЂР°Р·РјРµСЂС‹ С‡РµСЂРµР· Р·Р°РїСЏС‚СѓСЋ, РїСЂРѕР±РµР» РёР»Рё СЃ РЅРѕРІРѕР№ СЃС‚СЂРѕРєРё. РџСЂРёРјРµСЂ: 160x600, 240x400, 300x600" />
            <TextBox AcceptsReturn="True"
                     Height="140"
                     Text="{Binding SizesText, UpdateSourceTrigger=PropertyChanged}"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" />
        </StackPanel>

        <StackPanel Grid.Row="5"
                    Margin="0,16,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Width="110"
                    Margin="0,0,12,0"
                    Click="Cancel_Click"
                    Content="РћС‚РјРµРЅР°" />
            <Button Width="110"
                    Click="Save_Click"
                    Content="РЎРѕС…СЂР°РЅРёС‚СЊ" />
        </StackPanel>
    </Grid>
</Window>
